% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adfo.R
\name{adfoControl}
\alias{adfoControl}
\title{Control settings for the FO (First-Order) estimator}
\usage{
adfoControl(
  studies = list(),
  grad = c("none", "analytical", "fd", "cfd"),
  algorithm = "NLOPT_LN_BOBYQA",
  maxeval = 500L,
  ftol_rel = .Machine$double.eps^(1/2),
  print = 10L,
  seed = 12345L,
  cores = 1L,
  grad_h = 1e-04,
  grad_bounds = 5,
  cov_h = 0.001,
  cov_h_outer = .Machine$double.eps^(1/5),
  covMethod = c("r", "none"),
  n_restarts = 1L,
  restart_sd = 0.5,
  workers = 1L,
  rxControl = NULL,
  calcTables = FALSE,
  compress = TRUE,
  ci = 0.95,
  sigdig = 4,
  sigdigTable = NULL,
  addProp = c("combined2", "combined1"),
  optExpression = TRUE,
  sumProd = FALSE,
  literalFix = TRUE,
  returnAdmr = FALSE,
  ...
)
}
\arguments{
\item{studies}{Named list of study specifications (same format as
\code{\link[=admControl]{admControl()}}: \code{E}, \code{V}, \code{n}, \code{times}, \code{ev}, optional \code{method}).}

\item{grad}{Gradient mode.  \code{"none"} (default) uses derivative-free BOBYQA;
\code{"analytical"} uses the closed-form FO gradient (requires sensitivity
equations); \code{"fd"} uses forward finite differences of the full NLL;
\code{"cfd"} uses central finite differences for struct theta gradient
(more accurate than \code{"fd"}, roughly twice as many NLL evaluations per step).}

\item{algorithm}{nloptr algorithm.  Automatically coerced to
\code{"NLOPT_LD_LBFGS"} when \code{grad != "none"}.}

\item{maxeval}{Maximum function evaluations (default 500).}

\item{ftol_rel}{Relative tolerance (default \code{sqrt(.Machine$double.eps)}).}

\item{print}{Print-frequency for live progress (0 = silent).}

\item{seed}{Random seed (used for restarts).}

\item{cores}{OpenMP threads for \code{rxSolve()} (default 1).}

\item{grad_h}{Finite-difference step for unpaired struct theta gradient and
FD Jacobian.}

\item{grad_bounds}{Box-constraint half-width when using gradients.}

\item{cov_h}{Inner FD step for the gradient-based Hessian (only used when
\code{covMethod = "r"} and \code{grad != "none"}). Default 1e-3.}

\item{cov_h_outer}{Outer step scale for NLL-FD Hessian.}

\item{covMethod}{\code{"r"} computes covariance via numerical Hessian; \code{"none"}
skips it.}

\item{n_restarts}{Number of optimizer restarts (1 = no multi-start).}

\item{restart_sd}{Standard deviation for random perturbations of initial
struct thetas at each restart (> 1).}

\item{workers}{Number of parallel PSOCK/fork workers for multi-restart
(default 1 = sequential).}

\item{rxControl}{\code{rxode2::rxControl()} object. Created automatically when \code{NULL}.}

\item{calcTables, compress, ci, sigdig, sigdigTable, optExpression, sumProd, literalFix}{Passed to \code{nlmixr2est::foceiControl()} for the table/output machinery.}

\item{addProp}{How combined additive+proportional error is parameterised in
the nlmixr2 output tables: \code{"combined2"} (default, variance form) or
\code{"combined1"} (SD form). Has no effect on admixr2's own estimation.}

\item{returnAdmr}{If \code{TRUE}, return a plain list instead of the full
nlmixr2 fit object.}

\item{...}{Unused arguments (trigger an error).}
}
\value{
An \code{adfoControl} object (a named list).
}
\description{
Creates a control object for \code{nlmixr2(est = "adfo")}.  The FO estimator
linearises model predictions at \eqn{\eta = 0}: it is faster than the MC
estimator but less accurate for models with large IIV or strongly
non-linear individual predictions.
}
\examples{
# Inspect defaults
ctl <- adfoControl()
ctl$grad
ctl$maxeval

# Analytical gradient, more evaluations
ctl2 <- adfoControl(grad = "analytical", maxeval = 1000L)

\donttest{
library(rxode2)
library(nlmixr2)

data("examplomycin")
obs    <- examplomycin[examplomycin$EVID == 0, ]
obs    <- obs[order(obs$ID, obs$TIME), ]
times  <- sort(unique(obs$TIME))
ids    <- unique(obs$ID)
dv_mat <- do.call(rbind, lapply(ids, function(i) {
  sub <- obs[obs$ID == i, ]; sub$DV[order(sub$TIME)]
}))
E <- colMeans(dv_mat)
V <- cov.wt(dv_mat, method = "ML")$cov

pk_model <- function() {
  ini({
    tcl <- log(5); tv <- log(30)
    prop.sd <- c(0, 0.2)
    eta.cl ~ 0.09; eta.v ~ 0.04
  })
  model({
    cl <- exp(tcl + eta.cl)
    v  <- exp(tv  + eta.v)
    d/dt(central) <- -(cl/v) * central
    cp <- central / v
    cp ~ prop(prop.sd)
  })
}

fit <- nlmixr2(
  pk_model, admData(), est = "adfo",
  control = adfoControl(
    studies = list(study1 = list(E = E, V = V, n = length(ids),
                                 times = times, ev = et(amt = 100))),
    maxeval = 100L
  )
)
print(fit)
}

}
\seealso{
\code{\link[=admControl]{admControl()}}, \code{\link[=adirmcControl]{adirmcControl()}}
}
