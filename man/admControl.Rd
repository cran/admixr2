% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/admc.R
\name{admControl}
\alias{admControl}
\title{Control settings for the ADM estimator}
\usage{
admControl(
  studies = list(),
  n_sim = 5000L,
  sampling = c("sobol", "halton", "torus", "lhs", "rnorm"),
  algorithm = "NLOPT_LN_BOBYQA",
  maxeval = 500L,
  ftol_rel = .Machine$double.eps^2,
  print = 10L,
  seed = 12345L,
  cores = 1L,
  grad = c("sens", "fd", "cfd", "none"),
  grad_h = 1e-04,
  cov_h = 0.001,
  cov_h_outer = .Machine$double.eps^(1/5),
  grad_bounds = 5,
  covMethod = c("r", "none"),
  cov_n_sim = 10000L,
  n_restarts = 1L,
  restart_sd = 0.5,
  workers = 1L,
  rxControl = NULL,
  calcTables = FALSE,
  compress = TRUE,
  ci = 0.95,
  sigdig = 4,
  sigdigTable = NULL,
  addProp = c("combined2", "combined1"),
  optExpression = TRUE,
  sumProd = FALSE,
  literalFix = TRUE,
  returnAdmr = FALSE,
  ...
)
}
\arguments{
\item{studies}{Named list of study specifications. Each element is a list with:
\itemize{
\item \code{E} -- observed mean vector
\item \code{V} -- observed covariance matrix or variance vector (auto-detected)
\item \code{n} -- sample size
\item \code{times} -- numeric vector of observation times
\item \code{ev} -- \code{rxode2::et()} dosing event table
\item \code{method} -- \code{"cov"} or \code{"var"} (optional; auto-detected from \code{V})
}}

\item{n_sim}{Number of Monte Carlo samples per NLL evaluation.}

\item{sampling}{Sampling method for eta draws: \code{"sobol"} (Sobol, default),
\code{"halton"} (Halton), \code{"torus"} (Kronecker/torus), \code{"lhs"} (Latin hypercube),
or \code{"rnorm"} (iid normal).}

\item{algorithm}{nloptr algorithm string. Automatically switched to
\code{"NLOPT_LD_LBFGS"} when \code{grad != "none"}.}

\item{maxeval}{Maximum number of optimizer function evaluations.}

\item{ftol_rel}{Relative function-value tolerance for convergence.}

\item{print}{Print progress every this many evaluations (0 = silent).}

\item{seed}{Random seed for reproducibility.}

\item{cores}{Number of OpenMP threads for \code{rxSolve()}.}

\item{grad}{Gradient mode: \code{"sens"} (sensitivity equations, default), \code{"fd"}
(forward finite differences), \code{"cfd"} (central finite differences), or
\code{"none"} (derivative-free). A warning is issued when \code{"sens"} is requested
but the sensitivity model is unavailable; the estimator then falls back to
forward finite differences.}

\item{grad_h}{Step size for finite-difference gradient evaluation during
optimization (used by \code{grad = "fd"} or \code{"cfd"}). The default 1e-4 is near
the optimal balance between truncation error (grows with \code{h}) and MC noise
amplification (grows as \code{1/h}) for forward FD. Central FD (\code{"cfd"}) has a
slightly wider optimum around 1e-3, but 1e-4 works well for both.}

\item{cov_h}{Inner FD step for the gradient-based Hessian (only used when
\code{covMethod = "r"} and \code{grad != "none"}). Each gradient evaluation has MC
noise of order \code{sigma / cov_h}; the Hessian divides that noise by the outer
step, giving total noise \verb{sigma / (cov_h * cov_h_outer * |p|)}. \code{cov_h = 1e-3}
balances truncation error and noise amplification. Increase to \code{1e-2} if the
Hessian is non-positive definite.}

\item{cov_h_outer}{Outer step scale for the numerical Hessian. The actual step
for parameter \code{p} is \verb{max(|p|, 0.1) * cov_h_outer}. Applied to both the
gradient-FD Hessian (\code{grad != "none"}) and the NLL-FD Hessian
(\code{grad = "none"}). Default \code{eps^(1/5)} (~2.5e-3) is larger than the
textbook \code{eps^(1/4)} to account for MC noise in NLL and gradient evaluations;
empirically it matches the analytical (sensitivity-equation) Hessian ground
truth. Increase (e.g. to \code{5e-3} or \code{1e-2}) if the Hessian is non-positive
definite.}

\item{grad_bounds}{Box-constraint half-width when using gradients.}

\item{covMethod}{Covariance method: \code{"r"} (numerical Hessian) or \code{"none"}.}

\item{cov_n_sim}{Number of MC samples for the covariance (Hessian) step.
More samples reduce MC noise in NLL evaluations. The NLL-based Hessian
(\code{grad = "none"}) uses a central second difference of the NLL with the
same Sobol sequence (CRN) at every perturbed point, so noise largely
cancels and \code{cov_n_sim = 10000} (default) is sufficient for most models.}

\item{n_restarts}{Number of optimization restarts. Runs in parallel when
\code{workers > 1}.}

\item{restart_sd}{Standard deviation of structural theta perturbations for
restart initialisation.}

\item{workers}{Number of parallel workers for multi-restart. \code{1} (default)
runs restarts sequentially. Values \verb{> 1} use a PSOCK cluster on Windows
and fork workers on Unix/macOS. Workers are stopped automatically after
the restart phase so all cores are available for the Hessian step.}

\item{rxControl}{\code{rxode2::rxControl()} object. Created automatically when \code{NULL}.}

\item{calcTables, compress, ci, sigdig, sigdigTable, optExpression, sumProd, literalFix}{Passed to \code{nlmixr2est::foceiControl()} for the table/output machinery.}

\item{addProp}{How combined additive+proportional error is parameterised in
the nlmixr2 output tables: \code{"combined2"} (default, variance form) or
\code{"combined1"} (SD form). Has no effect on admixr2's own estimation; passed
to \code{nlmixr2est::foceiControl()} for the table/output machinery only.}

\item{returnAdmr}{If \code{TRUE}, return a plain list instead of a full nlmixr2
fit object (useful for debugging).}

\item{...}{Additional arguments (none allowed; triggers an error).}
}
\value{
An object of class \code{admControl}.
}
\description{
Constructs a control object for \code{est = "admc"}, the Monte Carlo aggregate
data modelling estimator.
}
\examples{
# Minimal control object -- inspect defaults
ctl <- admControl()
ctl$n_sim
ctl$algorithm

# Override key settings without fitting
ctl2 <- admControl(
  n_sim    = 2000L,
  maxeval  = 300L,
  grad     = "fd",
  seed     = 42L
)

\donttest{
library(rxode2)
library(nlmixr2)

data("examplomycin")
obs   <- examplomycin[examplomycin$EVID == 0, ]
obs   <- obs[order(obs$ID, obs$TIME), ]
times <- sort(unique(obs$TIME))
ids   <- unique(obs$ID)
dv_mat <- do.call(rbind, lapply(ids, function(i) {
  sub <- obs[obs$ID == i, ]; sub$DV[order(sub$TIME)]
}))
E <- colMeans(dv_mat)
V <- cov.wt(dv_mat, method = "ML")$cov

pk_model <- function() {
  ini({
    tcl <- log(5);  tv1 <- log(12); tv2 <- log(25)
    tq  <- log(12); tka <- log(1.2)
    prop.sd <- c(0, 0.2)
    eta.cl ~ 0.09; eta.v1 ~ 0.09; eta.v2 ~ 0.09
    eta.q  ~ 0.09; eta.ka ~ 0.09
  })
  model({
    cl <- exp(tcl + eta.cl); v1 <- exp(tv1 + eta.v1)
    v2 <- exp(tv2 + eta.v2); q  <- exp(tq  + eta.q)
    ka <- exp(tka + eta.ka)
    d/dt(depot)      <- -ka * depot
    d/dt(central)    <- ka * depot - (cl/v1 + q/v1) * central + (q/v2) * peripheral
    d/dt(peripheral) <- (q/v1) * central - (q/v2) * peripheral
    cp <- central / v1
    cp ~ prop(prop.sd)
  })
}

fit <- nlmixr2(
  pk_model, admData(), est = "admc",
  control = admControl(
    studies  = list(study1 = list(E = E, V = V, n = length(ids),
                                  times = times, ev = et(amt = 100))),
    n_sim    = 1000L,
    maxeval  = 200L
  )
)
print(fit)
}

}
